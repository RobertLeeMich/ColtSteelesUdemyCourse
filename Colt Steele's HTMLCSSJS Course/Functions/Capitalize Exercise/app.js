/* function capitalize(arg) {
    return arg.toUpperCase();
} */

//remember to put the () after calling a function so it runs. Herpaderpa.

function capitalize (str) {
    return str[0].toUpperCase() + str.slice(1);
}

//had to look up the solution to this since I didn't pay attention to instructions. 5 tries.
